// Package image provides high-level access to Harbor / docker-save image
// archives: metadata scanning, merged filesystem views, directory listing
// without extraction, selective extraction of single entries, and full
// merged rootfs extraction.
package image
