// Package parallels implements read-only access to Parallels virtual disk
// images (.pvm/.hds/.parallels), mapping the block-allocation table to data
// clusters, mirroring the layout QEMU's parallels driver reads.
package parallels
